# AWS Credentials Configuration
# Set these environment variables instead of using hardcoded credentials

# Option 1: Use AWS CLI configured credentials (Recommended)
# Ensure AWS CLI is configured with: aws configure
# This will use ~/.aws/credentials and ~/.aws/config

# Option 2: Set environment variables directly
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# Option 3: Use IAM roles (for EC2, ECS, Lambda)
# No additional configuration needed when running on AWS infrastructure

# Optional: Enable AWS SDK debugging
# AWS_SDK_LOAD_CONFIG=1

# Cognito Configuration
COGNITO_USER_POOL_ID=us-east-1_6IVaAL8X0
COGNITO_CLIENT_ID=4o3gl6qqe1i5uapeitu56p3lst

# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017/
MONGODB_DB_NAME=desktime

# Server Configuration
PORT=4005
NODE_ENV=dev
